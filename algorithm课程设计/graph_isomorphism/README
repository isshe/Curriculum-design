graph_degree.c: 
    定义计算图的各个顶点的度的函数
graph_free: 
    释放内存;
graph_malloc:
    申请内存;
graph_input:
    输入图的顶点;
graph_rand:
    计算图的秩;

此方法用的是:(a&b)
    a. 图的同构的必要条件:
        1). 边数相等;
        2). 顶点数相等;
        3). 各个顶点的度对应;

    b. 图的邻接矩阵的秩相同
